# Project State

## Now

Scheduled and commissioning briefs collect deterministically, synthesize once through OpenRouter,
validate fail-closed, render, and deploy to Pages. The presentation haircut and the final editorial
pass (checkpoint-driven masthead, bounded post-close history admission, symbol-named observed rows,
omitted empty sections, table-level as-of clocks, current-first chips, reader-only Basis, and the
v0.2 synthesis contract) are in place.

## Next

Observe several scheduled editions under the v0.2 contract. The compact synthesis projection
(`--compact-packet`, 68.5% smaller on the 2026-09-08 control evidence) is opt-in only: its one
paid A/B call on 2026-09-08 failed schema validation at the banner object because the payload
omitted the output schema and the transport did not enforce it. The schema is now kept in the
compact payload; a second A/B call needs the owner's decision.

## Later

Packet/token projection trimming, additional providers, charts, what-changed-since-prior-edition,
and run-directory naming by resolved checkpoint. Only after repeated editions read well.
